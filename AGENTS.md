# Project instructions

Preserve published Git history. Keep changes deployable and retain the existing Apex Velocity UI. Never commit secrets or expose privileged keys in browser code. Database RLS and transactional RPCs are authoritative for ownership, roles, totals and stock.
